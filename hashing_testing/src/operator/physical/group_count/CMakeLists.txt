# add_subdirectory(chained)
add_subdirectory(group_count_handler)
add_subdirectory(lp)
target_link_libraries(LP)
if(USE_AVX512)
    add_subdirectory(lcp)
    add_subdirectory(lp_horizontal)
    add_subdirectory(lp_vertical)
endif()